name: Release

# Triggered by a version tag: git tag v1.2.3 && git push --tags
on:
  push:
    tags: ['v*']

jobs:
  # ── Run full test suite first ───────────────────────────────────────────────
  test:
    name: Tests
    uses: ./.github/workflows/ci.yml

  # ── Publish CLI to npm ──────────────────────────────────────────────────────
  publish-cli:
    name: Publish to npm
    needs: test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm
          registry-url: https://registry.npmjs.org

      - run: pnpm install --frozen-lockfile

      - name: Publish webhooky
        run: npm publish --access public
        working-directory: apps/cli
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  # ── Build and push Docker image to Docker Hub ─────────────────────────────
  build-push-docker:
    name: Build & push Docker image
    needs: test
    runs-on: ubuntu-latest

    outputs:
      image: ${{ steps.meta.outputs.tags }}

    steps:
      - uses: actions/checkout@v6

      - uses: docker/setup-buildx-action@v4

      - uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - uses: docker/metadata-action@v6
        id: meta
        with:
          images: frulko/webhooky
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest

      - uses: docker/build-push-action@v7
        with:
          context: .
          file: apps/server/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ── Trigger Portainer redeploy via webhook ─────────────────────────────────
  deploy:
    name: Deploy via Portainer
    needs: build-push-docker
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Portainer webhook
        if: env.PORTAINER_WEBHOOK_URL != ''
        env:
          PORTAINER_WEBHOOK_URL: ${{ secrets.PORTAINER_WEBHOOK_URL }}
          PORTAINER_WEBHOOK_SECRET: ${{ secrets.PORTAINER_WEBHOOK_SECRET }}
        run: |
          ARGS=(-fsSL -X POST "$(echo "$PORTAINER_WEBHOOK_URL" | tr -d '[:space:]')")
          if [ -n "$PORTAINER_WEBHOOK_SECRET" ]; then
            ARGS+=(-H "x-webhook-secret: $PORTAINER_WEBHOOK_SECRET")
          fi
          curl "${ARGS[@]}"
